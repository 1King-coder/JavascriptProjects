// Arrays

const pessoas = ['Vitor', 'Lessa', 'Luciana', 'Renato', 'Alcilea', 'Ronnie Von']




// Retorna os objetos da forma (De, Até) [O item no índice Até não será exibido]
/*pessoas.slice(0, 3)
console.log(pessoas.slice(0, -3))*/

// Deleta o item no índice indicado e deixa um <empty item> no lugar.
// delete pessoas[2];


// Remove o PRIMEIRO elemento do array.
/*pessoas.shift();
let removido = pessoas.shift();
console.log(removido)*/

// Remove o ÚLTIMO elemento do array.
/*pessoas.pop();
let removido = pessoas.pop();
console.log(removido)*/

// Adiciona objeto no INÍCIO do array.
/*pessoas.unshift('Jorge');
pessoas.unshift('Luiza');*/

// Adiciona objeto no FIM do array.
//pessoas.push('Jorge');
//pessoas.push('Luiza');

// Adiciona objeto ao array.
/*pessoas[pessoas.length] = 'Jorge';
pessoas[pessoas.length] = 'Luiza';*/

// Edita o objeto no índice indicado.
/*pessoas[0] = 'Marcos';  
pessoas[1] = 'Joana';*/

console.log(pessoas);